<view class="container" wx:if="{{order}}">
  <view class="info-card card">
    <view class="info-header">
      <view class="order-no">{{order.orderNo}}</view>
      <view class="debt-badge {{order.isDebt === 1 ? 'debt' : 'cash'}}">
        {{order.paymentTypeName}}
      </view>
    </view>

    <view class="info-body">
      <view class="info-row">
        <view class="info-label">客户</view>
        <view class="info-value">{{order.customerName}}</view>
      </view>
      <view class="info-row">
        <view class="info-label">果品名称</view>
        <view class="info-value">{{order.productName}}</view>
      </view>
      <view class="info-row" wx:if="{{order.variety}}">
        <view class="info-label">品种</view>
        <view class="info-value">{{order.variety}}</view>
      </view>
      <view class="info-row" wx:if="{{order.grade}}">
        <view class="info-label">等级</view>
        <view class="info-value">{{order.grade}}</view>
      </view>
      <view class="info-row">
        <view class="info-label">重量</view>
        <view class="info-value">{{order.weight}}{{order.unit}}</view>
      </view>
      <view class="info-row">
        <view class="info-label">销售单价</view>
        <view class="info-value">¥{{order.unitPrice}}/{{order.unit}}</view>
      </view>
      <view class="info-row">
        <view class="info-label">成本单价</view>
        <view class="info-value">¥{{order.costPrice}}/{{order.unit}}</view>
      </view>
      <view class="info-row highlight">
        <view class="info-label">利润</view>
        <view class="info-value text-primary">¥{{order.profit}}</view>
      </view>
      <view class="info-row total">
        <view class="info-label">总金额</view>
        <view class="info-value text-danger">¥{{order.totalAmount}}</view>
      </view>
      <view class="info-row">
        <view class="info-label">出库时间</view>
        <view class="info-value">{{order.outboundTime}}</view>
      </view>
      <view class="info-row" wx:if="{{order.operator}}">
        <view class="info-label">操作人</view>
        <view class="info-value">{{order.operator}}</view>
      </view>
      <view class="info-row" wx:if="{{order.remark}}">
        <view class="info-label">备注</view>
        <view class="info-value">{{order.remark}}</view>
      </view>
      <view class="info-row">
        <view class="info-label">创建时间</view>
        <view class="info-value">{{order.createTime}}</view>
      </view>
      <view class="info-row" wx:if="{{order.operatorName}}">
        <view class="info-label">操作人</view>
        <view class="info-value">{{order.operatorName}}</view>
      </view>
    </view>
  </view>
</view>

<view class="action-bar" wx:if="{{order}}">
  <view class="action-btn delete" bindtap="deleteOrder">
    <text class="action-icon">🗑️</text>
    <text class="action-text">删除</text>
  </view>
</view>
